/**
 * Group tournament (prediction) screen rendered inside the Tournaments stack.
 * Mirrors /(tabs)/groups/tournament/[slug] so that navigating from a tournament
 * detail page keeps the back button within the Tournaments stack.
 */
export { default } from '../../groups/tournament/[slug]';
